package designPattern.creational;

public class FactoryMethod {
    // abstract method create()
    // can be thought as opposite of singleton as factoryMethod retursn multiple copies
    // defers creation to subclass
}
